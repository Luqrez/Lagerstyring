// Dashboard component: Serves as the main landing page after user login.
// Intended for displaying key business metrics, summaries, or navigation to core features.

function Dashboard() {
    return (
        <div>
            {/* Header for the dashboard page.
                This can be extended to include widgets, charts, and KPIs as needed for business requirements. */}
            <h1>Dashboard Page!</h1>
        </div>
    );
}

export default Dashboard;
